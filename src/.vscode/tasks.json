{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Services/Identity.API/Identity.API/Identity.API.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}